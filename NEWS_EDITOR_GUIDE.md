# সংবাদ তৈরি / সম্পাদনা — ব্যবহার নির্দেশিকা

এই গাইডটি অ্যাডমিন প্যানেলের **“নতুন সংবাদ যোগ করুন / সংবাদ সম্পাদনা”** পেজের জন্য।
ডিজাইন (রঙ, লোগো, ফন্ট, সাইডবার) আগের মতোই আছে — শুধু কাজ করার সুবিধা বেড়েছে।

---

## ১. ফিল্ডগুলো (সব এক পেজে)

| অংশ | কী আছে | নিয়ম |
|---|---|---|
| মূল তথ্য | শিরোনাম (৩–১৯১ অক্ষর), স্লাগ (খালি রাখলে শিরোনাম থেকে), ক্যাটাগরি\*, সাব-ক্যাটাগরি, রিপোর্টার | ক্যাটাগরি বদলালে সাব-ক্যাটাগরির তালিকা নিজে থেকে বদলায় |
| সংক্ষিপ্ত বিবরণ | Short Description | খালি রাখলে মূল লেখা থেকে তৈরি; কার্ড/সার্চ/শেয়ারে দেখায় |
| বিস্তারিত সংবাদ | টুলবার সহ এডিটর (H2/H3, বোল্ড, ইটালিক, তালিকা, উদ্ধৃতি, লিংক, ছবি, লাইভ প্রাকদর্শন, বড় করে লেখা) | কমপক্ষে ২০ অক্ষর; অক্ষর ও শব্দ সংখ্যা লাইভ দেখা যায় |
| ছবি | ফিচার্ড ইমেজ (ক্যাপশন + ক্রেডিট সহ), একাধিক গ্যালারি ছবি | jpg/jpeg/png/webp, প্রতিটি সর্বোচ্চ `MC_UPLOAD_MAX_KB` (ডিফল্ট ৪ MB), একসাথে `MC_UPLOAD_MAX_FILES` (ডিফল্ট ২০) |
| গ্যালারি অপারেশন | টেনে আনার (drag & drop) মাধ্যমে ক্রম বদল, ▲▼ বাটন, ★ = ফিচার্ড করা, ✕ = মুছে ফেলা, ক্যাপশন/ক্রেডিট/Alt এডিট | ক্রম বদলালে সাথে সাথে সংরক্ষিত হয় |
| প্রকাশ | স্ট্যাটাস (খসড়া/রিভিউতে/প্রকাশিত/নির্ধারিত/আর্কাইভড), প্রকাশের তারিখ ও সময়, শিডিউল, হোমপেজে দেখান, ব্রেকিং নিউজ, মন্তব্যের অনুমতি | শিডিউলের সময় অবশ্যই ভবিষ্যতে হতে হবে |
| SEO | মেটা টাইটেল, মেটা বিবরণ, কীওয়ার্ড, OG টাইটেল/বিবরণ/ছবি, Canonical | খালি রাখলে শিরোনাম/সংক্ষিপ্ত বিবরণ থেকে স্বয়ংক্রিয় |
| ট্যাগ | লিখে Enter → চিপ যোগ; ✕ দিয়ে ডিলিট; পুরোনো ট্যাগ সাজেশন | ট্যাগ পেজে প্রতিটি ট্যাগের নিজস্ব SEO লিংক তৈরি হয় |

\* ক্যাটাগরি ছাড়া সংবাদ সংরক্ষণ করা যায় না।

## ২. সংরক্ষণের বাটনগুলো

| বাটন | কী হয় |
|---|---|
| **খসড়া সংরক্ষণ** | `draft` — ওয়েবসাইটে দেখা যাবে না (প্রাকদর্শন করা যাবে) |
| **রিভিউতে পাঠান** | `pending` — রিপোর্টারের ডিফল্ট; এডিটর অনুমোদন দিলে প্রকাশিত হবে |
| **প্রকাশ করুন / হালনাগাদ ও প্রকাশ** | `published` — **সাথে সাথে ওয়েবসাইটে** (ক্যাশ স্বয়ংক্রিয়ভাবে রিফ্রেশ হয়) |
| **শিডিউল** | `scheduled` — নির্ধারিত তারিখ-সময়ে স্বয়ংক্রিয়ভাবে প্রকাশিত |
| **ট্র্যাশে পাঠান** | Soft delete (রিসাইকেল বিন থেকে ফেরানো যায়) |

> রিপোর্টারের `news.publish` অনুমতি না থাকলে “প্রকাশ/শিডিউল” বাটন দেখা যায় না;
> কোনোভাবে চেষ্টা করলেও সংবাদটি `pending`-এ পড়ে (তথ্য হারায় না)।

## ৩. ছবি কীভাবে যুক্ত হয় (নিরাপদ ধারা)

```
ছবি নির্বাচন (ফাইল) → Laravel Backend (ভ্যালিডেশন) → ImgBB API (Key শুধু .env থেকে)
                    → MySQL-এ শুধু রেফারেন্স  → সাইটে signed proxy URL (/img/…)
```

> **ছবি দেওয়ার একমাত্র উপায় সরাসরি ফাইল আপলোড।** অ্যাডমিন প্যানেলে ছবির জন্য
> কোনো “Image URL / Link” ইনপুট নেই (আগের ইনপুট ও লজিক সম্পূর্ণ সরানো হয়েছে) —
> কেউ URL পাঠানোর চেষ্টা করলে সার্ভার সেটি গ্রহণ করে না।

* অ্যাডমিন বা পাবলিক পেজে **raw hosting URL কখনো দেখা যায় না** — শুধু থাম্বনেইল/প্রিভিউ।
* ছবিটি DB-তে BLOB হিসেবে নয়, শুধু রেফারেন্স হিসেবে থাকে; তাই হোস্টিং ভবিষ্যতে বদলানো সহজ।
* এডিটরে ছবি যোগ করলে কনটেন্টে `{{media:ID}}` রেফারেন্স বসে, render সময় signed URL তৈরি হয়।
* ImgBB API ব্যর্থ হলে (Key ভুল/বাতিল/সার্ভিস বন্ধ) স্পষ্ট বাংলা বার্তা দেখায় — কোনো নীরব ব্যর্থতা নয়।
* `IMGBB_API_KEY` না থাকলে ছবি `public/uploads` এ যাবে (Fallback), যদি `IMAGE_LOCAL_FALLBACK=true` থাকে।

## ৪. প্রাকদর্শন

* **প্রাকদর্শন (মোডাল)** — সংরক্ষণের আগেই বর্তমান তথ্য দিয়ে সংবাদ কেমন দেখাবে (ডেস্কটপ/মোবাইল)।
* **নতুন ট্যাবে** — সংরক্ষীত সংবাদের পূর্ণ পাবলিক ভিউ (`noindex` — গুগলে যাবে না), সাথে “সম্পাদনায় ফিরুন” লিংক।

## ৫. দ্রুত টিপস

1. শুরুতে শিরোনাম লিখুন → স্লাগ নিজেই তৈরি হবে।
2. সংক্ষিপ্ত বিবরণ ১৬০ অক্ষরের মধ্যে রাখলে কার্ড/সার্চে সুন্দর দেখায়।
3. ফিচার্ড ইমেজ ১২০০×৬৭৫ (১৬:৯) হলে সবচেয়ে ভালো দেখায়।
4. গ্যালারির কোনো ছবি ★ করলে সেটি খবরের প্রধান ছবি হয়ে যায় (আগেরটি সাধারণ গ্যালারি ছবি থাকে)।
5. সেভ করার পর পেজটি এডিট মোডে ফিরে আসে — সেখানে “হালনাগাদ & প্রকাশ” দিয়ে আবার সেভ করা যায়।
6. বড় লেখা লিখতে টুলবারের ⤢ (বড় করে লিখুন) বাটন ব্যবহার করুন।
7. ভুল হলে `Ctrl+Z` কাজ করে না — গুরুত্বপূর্ণ লেখা নিয়মিত সেভ করুন (খসড়া হিসেবে)।

## ৬. পঠনসংখ্যা (View Count) — শুধু বাস্তব ভিজিট

**ডেমো/ফেক সংখ্যা সম্পূর্ণ বাদ।** নিয়ম:

```
News Publish → ০ Views → Visitor দেখল → ১ → আরেকজন দেখল → ২ …
```

* নতুন সংবাদ প্রকাশিত হয় **০ ভিউ** নিয়ে; সংখ্যা বাড়ে শুধু সত্যিকারের পাঠক পেজ খুললে।
* **ডুপ্লিকেট প্রোটেকশন (দুই স্তর):** একই ব্রাউজার সেশনে একটি সংবাদ একবারই গোনা হয়;
  সেশন মুছে গেলেও একই ভিজিটর `MC_VIEW_DEDUPE_MINUTES` (ডিফল্ট ১২ ঘণ্টা) পর্যন্ত
  আবার গোনা হয় না ⇒ F5 চেপে বা স্ক্রিপ্ট দিয়ে সংখ্যা বাড়ানো যায় না।
* **বট/ক্রলার গোনা হয় না** (Googlebot, Facebook/WhatsApp লিংক প্রিভিউ, মনিটরিং টুল)।
  দরকার হলে `.env` এ `MC_COUNT_BOT_VIEWS=true`।
* খসড়া/শিডিউল করা সংবাদ ও অ্যাডমিন প্রিভিউ পেজ ভিউ হিসাবে গোনা হয় না (৪০৪/প্রিভিউ বাদ)।
* সংখ্যা Database-এ প্রতিটি সংবাদের জন্য আসল মান হিসেবে জমা থাকে; অ্যাডমিন ও
  ওয়েবসাইট — দুই জায়গাতেই সেই একই সংখ্যা দেখায়।
* অ্যাডমিন থেকে সংখ্যা হাতে এডিট করা যায় না (`views` ফিল্ড নেই, খসড়ায় ০ থেকে শুরু)।

> পুরোনো ডেমো/সিড ডেটার ভুয়া সংখ্যা মাইগ্রেশনে একবার শূন্য করা হয়:
> `php artisan migrate` (মাইগ্রেশন `2026_09_19_000030_reset_fake_view_counts`)।

**কনফিগ (.env):** `MC_VIEW_DEDUPE_MINUTES=720` · `MC_COUNT_BOT_VIEWS=false`

## ৭. অ্যাডমিন ট্রাবলশুটিং (টেকনিক্যাল)

| সমস্যা | কারণ / সমাধান |
|---|---|
| “ছবি হোস্টিং কনফিগার করা নেই” | `.env` এ `IMGBB_API_KEY` + `IMGBB_ENABLED=true` দিন → `php artisan optimize:clear` |
| ছবি ভাঙা দেখাচ্ছে | `IMAGE_PROXY_ENABLED=true` কিনা এবং হোস্টিং সার্ভার থেকে ছবি আসছে কিনা দেখুন |
| পরিবর্তন সাইটে দেখা যাচ্ছে না | Normal নয় — Post সেভ করলেই ক্যাশ রিফ্রেশ হয়; একবার `php artisan cache:clear` চালান |
| ৫০০ এরর | `storage/logs/laravel.log` দেখুন |

**আরও:** `IMGBB_SETUP.md` (ইমেজ সিস্টেম), `PHP_ONLY_DEPLOY.md` (cPanel deploy)।
