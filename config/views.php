<?php

/**
 * ==========================================================================
 * View Counter — বাস্তব পঠনসংখ্যা (Real View Count)
 * ==========================================================================
 * নীতিমালা:
 *   - সংবাদ প্রকাশিত হওয়ার সময় ভিউ = ০; সব হিসাব শুধু বাস্তব ভিজিট থেকে।
 *   - DB-তে প্রতিটি সংবাদের আসল সংখ্যা জমা থাকে (ব্লোব/ডেমো সংখ্যা নয়)।
 *   - একই ভিজিটর সংবাদটি বারবার রিফ্রেশ করলেও বারবার গোনা হয় না
 *     (সেশন + কুলডাউন — দুটি স্তরে ডিডুপ্লিকেশন)।
 *
 * কনফিগারেশন (.env):
 *   MC_VIEW_DEDUPE_MINUTES=720   ← একই ভিজিটর কত মিনিটে একবার গোনা হবে (ডিফল্ট ১২ ঘণ্টা)
 *   MC_COUNT_BOT_VIEWS=false     ← true করলে ক্রলার/বট ভিজিটও গোনা হবে (সাধারণত দরকার নেই)
 */

return [

    /*
    |--------------------------------------------------------------------------
    | ডিডুপ্লিকেশন কুলডাউন (মিনিট)
    |--------------------------------------------------------------------------
    | একই ভিজিটর (একই IP + ব্রাউজার) একটি সংবাদ নির্দিষ্ট সময়ের মধ্যে একবারই
    | গোনা হয়। সেশন শেষ হয়ে গেলেও এই সময় পর্যন্ত নতুন ভিউ যোগ হবে না —
    | ফলে F5 চেপে বা স্ক্রিপ্ট দিয়ে সংখ্যা বাড়ানো যায় না।
    */
    'dedupe_minutes' => (int) env('MC_VIEW_DEDUPE_MINUTES', 720),

    /*
    |--------------------------------------------------------------------------
    | বট/ক্রলার ভিজিট গোনা হবে কি?
    |--------------------------------------------------------------------------
    | ডিফল্ট false — গুগলবট, ফেসবুক প্রিভিউ, মনিটরিং টুল ইত্যাদি ভিজিট হিসেবে
    | গোনা হয় না, নইলে পঠনসংখ্যা কৃত্রিমভাবে ফুলে যায়।
    */
    'count_bots' => (bool) env('MC_COUNT_BOT_VIEWS', false),
];
